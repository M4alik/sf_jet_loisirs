{% extends 'base.html.twig' %}

{% block title %}Creneau index{% endblock %}

{% block body %}
{% if planning.status == true %}
    <h1>Creneaux</h1>

    {# <table class="table">
        <thead>
            <tr>
                <th>Status</th>
                <th>Date_debut</th>
                <th>Date_fin</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody> #}
<div class="container mb-5 text-center">
    <div class="row row-cols-1 row-cols-md-4 g-5">
    {% for creneau in creneaus %} 
        <div class="col">
            <div class="card h-100" style="width: 16rem;">
                <div class="card-body">
                    <h5 class="card-title">{{ creneau.status ? 'Réservé' : 'Disponible' }}</h5>
                    <h6 class="card-subtitle mb-2 text-muted">{{ creneau.dateDebut ? creneau.dateDebut|date('d / m / Y H:i:s') : '' }}</h6>
                    <h6 class="card-subtitle mb-2 text-muted">{{ creneau.dateFin ? creneau.dateFin|date('H:i:s') : '' }}</h6>
                    {% if (creneau.status == false) %}
                        <a href="{{ path('app_creneau_confirmer', {'creneau': creneau.id}) }}">Choisir creneau</a>
                    {% endif %}
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
    {% else %}
    <div>
        no records found
    </div>
    
</div>   
    
            {# <tr>
                <td>{{ creneau.status ? 'Réservé' : 'Disponible' }}</td>
                <td>{{ creneau.dateDebut ? creneau.dateDebut|date('d / m / Y H:i:s') : '' }}</td>
                <td>{{ creneau.dateFin ? creneau.dateFin|date('H:i:s') : '' }}</td>
                <td>
                {% if(creneau.status == false) %}
                    <a href="{{ path('app_creneau_confirmer', {'creneau': creneau.id}) }}">Choisir creneau</a>
                {% endif %}
                </td>
            </tr> #}
        {# {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        
        </tbody>
    </table> #}
{# {% else %} #}
<h1>Ce planning est inactif</h1>
{% endif %}

{% endblock %}
