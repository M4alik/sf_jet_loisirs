{% extends 'base.html.twig' %}

{% block title %}Hello ReserverController!{% endblock %}

{% block body %}
    <h1>Choisissez votre activité</h1>

    <table class="table">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Nom</th>
                <th>Description</th>
                {# <th>Status</th> #}
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for activity in activities %}
            <tr>
                {# <td>{{ activity.id }}</td> #}
                <td>{{ activity.Name }}</td>
                <td>{{ activity.description }}</td>
                {# <td>{{ activity.status ? 'Yes' : 'No' }}</td> #}
                <td>
                    <a href="{{ path('app_reserver_planning', {'activity': activity.id}) }}">Choisir</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
